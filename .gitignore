# Sensitive - don't sync
auth.json

# Personal instructions - don't sync
APPEND_SYSTEM.md

# Local state - don't sync
.pi/
sessions/
bin/


pi-crash.log

# Pi runtime files (created when this repo is ~/.pi/agent/)
git/
mcp-cache.json
mcp-npx-cache.json
run-history.jsonl
session-manager-config.toml

# Extension dependencies (install locally)
extensions/*/node_modules/

# OS files
.DS_Store
Thumbs.db
tmp/

# The context-attribution focused tests import the globally installed pi package.
# The committed symlink resolves it for node --test without a manual install step.
!extensions/context-attribution/node_modules/
!extensions/context-attribution/node_modules/@earendil-works/
!extensions/context-attribution/node_modules/@earendil-works/pi-coding-agent
